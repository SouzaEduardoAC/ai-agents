description = "Squad Orchestrator: Approve a specific pipeline gate to unblock the next phase."

prompt = """
# Action: Register Human Approval for Pipeline Gate
Gate to approve: {{args}}

You MUST call the MCP tool:
```
tech_agents:pipeline_approve(gate="{{args}}")
```

This tool automatically resolves the branch-specific pipeline state file at the root of the active workspace and marks the gate as approved.
Report the exact output returned by the tool directly to the user.
"""
